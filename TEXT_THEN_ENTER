#include <Keyboard.h>

void setup() {
  Keyboard.begin();

  delay(500);  // Give Windows time to recognize the keyboard

  Keyboard.print("INPUT-HERE");
  delay(500);
  Keyboard.press(KEY_RETURN);
  Keyboard.release(KEY_RETURN);


  Keyboard.end();
}

void loop() {
}
